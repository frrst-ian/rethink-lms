{
  "name": "rethink-lms",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/client\" \"npm run dev -w packages/server\"",
    "dev:client": "npm run dev -w packages/client",
    "dev:server": "npm run dev -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "dotenv": "^17.3.1"
  }
}
